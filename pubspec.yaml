name: cleancity # <--- C'EST CETTE LIGNE QUI MANQUE SUREMENT
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0' # Vérifiez que votre version de SDK est correcte ici

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Vos images sont ici
  assets:
    - assets/home.jpg